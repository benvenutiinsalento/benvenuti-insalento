const form=document.querySelector('#submission-form'),out=document.querySelector('#submission-status');form.addEventListener('submit',async e=>{e.preventDefault();out.textContent='Invio in corso…';const body=Object.fromEntries(new FormData(form));try{const r=await fetch('/api/event-submissions',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)}),d=await r.json();if(!r.ok)throw new Error(d.error?.message||'Errore');out.textContent=d.message;form.reset()}catch(e){out.textContent=e.message||'Invio non riuscito.'}});
