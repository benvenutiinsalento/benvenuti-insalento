import { requireRole } from './_shared/auth.mjs';import { bootstrapSystem,syncIpaOfficialWebsites,bootstrapStatus } from './_shared/bootstrap.mjs';import { json,error } from './_shared/http.mjs';
export default async(req)=>{const a=await requireRole(req,'admin');if(!a.ok)return error(a.message,a.status,'UNAUTHORIZED');if(req.method==='GET')return json({ok:true,status:await bootstrapStatus()});if(req.method!=='POST')return error('Metodo non consentito',405,'METHOD_NOT_ALLOWED');try{const bootstrap=await bootstrapSystem();let ipa=null;try{ipa=await syncIpaOfficialWebsites();}catch(e){ipa={error:String(e.message||e)}}return json({ok:true,bootstrap,ipa,status:await bootstrapStatus()});}catch(e){return error('Bootstrap fallito',500,'BOOTSTRAP_FAILED',String(e.message||e));}};
export const config={path:'/api/admin/bootstrap'};
