import { requireRole } from './_shared/auth.mjs';import { runIngestionBatch } from './_shared/ingestion.mjs';import { calculateCoverage } from './_shared/coverage.mjs';import { syncIpaOfficialWebsites } from './_shared/bootstrap.mjs';import { json,error } from './_shared/http.mjs';
export default async(req)=>{const a=await requireRole(req,'admin');if(!a.ok)return error(a.message,a.status,'UNAUTHORIZED');if(req.method!=='POST')return error('Metodo non consentito',405,'METHOD_NOT_ALLOWED');try{const b=await req.json(),action=b.action||'ingest';if(action==='coverage')return json({ok:true,result:await calculateCoverage()});if(action==='ipa')return json({ok:true,result:await syncIpaOfficialWebsites()});return json({ok:true,result:await runIngestionBatch({limit:Math.min(20,Math.max(1,Number(b.limit)||8)),sourceIds:Array.isArray(b.sourceIds)?b.sourceIds:[],runType:'manual'})});}catch(e){return error('Esecuzione fallita',500,'TRIGGER_FAILED',String(e.message||e));}};
export const config={path:'/api/admin/trigger'};
